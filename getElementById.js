// returns the document object/outline provided by the browser: #document
    // >document

// grabbing element from the document
    // >document.getElementById('page-banner')

// reach into the DOM: stores element within variable
var banner = document.getElementById('page-banner');
    // >banner
var bookList = document.getElementById('book-list');
    // >bookList
